# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

RookFields is a research project extending DSGRN (Dynamic Signatures Generated by Regulatory Networks) with utilities for analyzing and visualizing regulatory networks. The project focuses on computational biology applications, particularly:
- Boolean network dynamics analysis
- Morse graph computation and visualization
- Phase space analysis of gene regulatory networks
- Integration with CoLoMoTo tools (ginsim, biolqm, boolsim)
- Conversion between DSGRN parameters and boolean/truth table representations

## Repository Structure

The repository contains:
- `src/DSGRN_utils/`: Main utilities package for DSGRN extensions
- `DSGRN/`: DSGRN library (git submodule/external dependency)
- `DSGRN_utils/`: Additional DSGRN utilities (separate git repository)
- `notebooks/`: Jupyter notebooks for analysis and figure generation
  - Boolean network analysis with CoLoMoTo tools
  - Truth table to GINsim conversions
  - Figure generation for publications

## Development Setup

```bash
# Install in development mode
pip install -e .

# Core dependencies
pip install DSGRN pyCHomP2
```

### Running Boolean Network Notebooks

Notebooks using CoLoMoTo tools (ginsim, biolqm, boolsim) require the CoLoMoTo Docker environment.

**Recommended method:**

```bash
# From repository root
colomoto-docker -v notebooks:local-notebooks
```

This opens JupyterLab in your browser with notebooks accessible in the `local-notebooks` directory.

See `TUTORIAL.md` for detailed instructions, troubleshooting, and alternative methods.

## Architecture and Key Components

### Core Modules

The package is organized under `src/DSGRN_utils/` with the following key components:

1. **Morse Graph Analysis**
   - `ComputeMorseGraph.py`: Computes Morse graphs from network parameters using Conley theory
   - `MorseGraph.py`: Core Morse graph data structures and algorithms
   - `PlotMorseGraph.py`: Visualizes Morse graph structure with networkx
   - `PlotMorseSets.py`: Visualizes Morse sets in phase space projections

2. **State Space Analysis**
   - `CubicalBlowupGraph.py`: Creates state transition graphs from regulatory network parameters
   - `WallLabelling.py`: Handles wall labellings for phase space decomposition
   - `RampFunction.py`: Implements ramp systems functionality for regulatory dynamics

3. **Query Tools**
   - `StabilityQuery.py`: Analyzes stability of attractors across parameter space
   - `IsomorphismQuery.py`: Checks for graph isomorphisms in network structures

### Key Integration Points

- All visualization functions expect DSGRN parameter objects and return matplotlib figures
- Morse graph computations use pyCHomP2 for topological analysis
- Functions typically accept DSGRN.Parameter objects and return specialized data structures

## Working with Notebooks

The `notebooks/` directory contains computational experiments:
- Use Jupyter notebooks for exploratory analysis and figure generation
- Boolean network notebooks integrate with CoLoMoTo tools (ginsim, biolqm, boolsim)
- Recent work focuses on boolean DSGRN parameters and conversions to truth tables

### Common Workflow

```python
import DSGRN
import DSGRN_utils

# Define network
network = DSGRN.Network("v1 : ~v2\nv2 : ~v1")
parameter_graph = DSGRN.ParameterGraph(network)
parameter = parameter_graph.parameter(index)

# Compute and visualize Morse graph
morse_graph, stg, graded_complex = DSGRN_utils.ComputeMorseGraph.ConleyMorseGraph(parameter)
DSGRN_utils.PlotMorseGraph(morse_graph)
DSGRN_utils.PlotMorseSets(morse_graph, stg, graded_complex)
```

### Figure Generation

- Save figures to `figures/` directory
- Recent commits include figure generation for boolean DSGRN paper
- Use descriptive filenames with parameter information

## Important Implementation Notes

1. **Performance Considerations**: Morse graph computations can be expensive for large networks. Include timing measurements when running computational experiments.

2. **Visualization**: PlotMorseSets uses specific color settings and arrow styles. Maintain consistency for publication figures.

3. **Parameter Indexing**: DSGRN uses specific parameter indexing schemes. Use `DSGRN.index_from_partial_orders()` when working with partial orders for correct parameter selection.

4. **Boolean Networks**: Recent work includes methods to convert boolean parameters to truth table/GINsim formats. See `notebooks/tt_to_ginsim.ipynb` and related notebooks.

5. **Git Submodules**: DSGRN/ and DSGRN_utils/ directories are separate git repositories. Changes to these should be committed in their respective repositories.